{
  "name": "funlab-firebase-functions",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "main": "src/index.js",
  "scripts": {
    "check": "node --check src/index.js && node --check src/config/agentConfig.js && node --check src/lib/promptFlow.js",
    "serve": "npx firebase-tools emulators:start --only functions,firestore",
    "deploy": "npx firebase-tools deploy --only functions,firestore"
  },
  "dependencies": {
    "@google/genai": "^1.44.0",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.1.0"
  }
}
